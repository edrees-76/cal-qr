<Window x:Class="CAL_QR.Views.Dialogs.AlertPopupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="تنبيهات الصلاحية النشطة" Height="280" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        FlowDirection="RightToLeft">

    <Border Background="White" CornerRadius="12" BorderBrush="#E2E8F0" BorderThickness="1.5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#C62828" CornerRadius="10,10,0,0" Padding="15,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Grid.Column="0" Kind="AlertDecagram" Foreground="White" Width="24" Height="24" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <TextBlock Grid.Column="1" Text="تنبيه الأجهزة منتهية الصلاحية" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" FontFamily="{StaticResource CairoFont}" />
                </Grid>
            </Border>

            <!-- Content -->
            <Border Grid.Row="1" Padding="20">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="لقد تم رصد أجهزة تتطلب انتباهاً عاجلاً!" FontSize="14" FontWeight="Bold" Foreground="#2D3748" HorizontalAlignment="Center" Margin="0,0,0,15" FontFamily="{StaticResource CairoFont}" />
                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                        <TextBlock Text="إجمالي التنبيهات النشطة: " FontSize="13" Foreground="#4A5568" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding TotalAlerts}" FontSize="15" FontWeight="Bold" Foreground="#C62828" Margin="5,0" />
                        <TextBlock Text=" جهاز" FontSize="13" Foreground="#4A5568" FontFamily="{StaticResource CairoFont}" />
                    </StackPanel>

                    <TextBlock Text="يرجى مراجعة لوحة البيانات للتعرف على قائمة الأجهزة المنتهية أو قريبة الانتهاء لتفادي استخدامها في عمليات القياس." FontSize="11" Foreground="#718096" TextWrapping="Wrap" TextAlignment="Center" FontFamily="{StaticResource CairoFont}" />
                </StackPanel>
            </Border>

            <!-- Footer Buttons -->
            <Border Grid.Row="2" Background="#F8FAFC" CornerRadius="0,0,10,10" Padding="15,12" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    
                    <Button Grid.Column="0" Content="عرض التفاصيل 📊" Style="{StaticResource MaterialDesignRaisedButton}" Background="#1A3A6B" BorderBrush="#1A3A6B" Height="36" Margin="0,0,5,0" Click="ShowDetailsButton_Click" FontFamily="{StaticResource CairoFont}" />
                    <Button Grid.Column="1" Content="إغلاق" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#4A5568" BorderBrush="#CBD5E1" Height="36" Margin="5,0,0,0" Click="CloseButton_Click" FontFamily="{StaticResource CairoFont}" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>

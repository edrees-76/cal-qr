<Window x:Class="CAL_QR.Views.Dialogs.PaperTemplateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="قالب طباعة جديد" 
        SizeToContent="Height"
        Width="800"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Style for Field Labels -->
        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#333333" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,5" />
            <Setter Property="FontFamily" Value="{StaticResource CairoFont}" />
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="إعدادات قالب الورق والملصقات" FontSize="16" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,15" FontFamily="{StaticResource CairoFont}" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Right Column (Column 0 in RTL, showing first on the right) -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="اسم القالب" Style="{StaticResource FieldLabelStyle}" />
                    <TextBox Text="{Binding TemplateName, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: ملصقات A4 3x8" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" Margin="0,0,0,10" FontFamily="{StaticResource CairoFont}" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,5,10">
                            <TextBlock Text="نوع الورق" Style="{StaticResource FieldLabelStyle}" />
                            <ComboBox SelectedValue="{Binding PaperType}" SelectedValuePath="Tag" Style="{StaticResource MaterialDesignOutlinedComboBox}" Height="40" FontFamily="{StaticResource CairoFont}">
                                <ComboBoxItem Content="A4 ورق ملصقات" Tag="A4" />
                                <ComboBoxItem Content="رول مستمر (Roll)" Tag="Roll" />
                                <ComboBoxItem Content="ورق مخصص" Tag="Custom" />
                            </ComboBox>
                        </StackPanel>

                        <CheckBox Grid.Column="1" IsChecked="{Binding IsDefault}" Content="تعيين كقالب افتراضي" VerticalAlignment="Center" Margin="5,20,0,0" FontFamily="{StaticResource CairoFont}" />
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,5,10">
                            <TextBlock Text="عرض الورقة (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding PaperWidth, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 210" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5,0,0,10">
                            <TextBlock Text="ارتفاع الورقة (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding PaperHeight, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 297" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,5,10">
                            <TextBlock Text="عدد الأعمدة" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding Columns, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 3" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5,0,0,10">
                            <TextBlock Text="عدد الصفوف" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding Rows, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 8" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!-- Left Column (Column 1 in RTL, showing on the left) -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,5,10">
                            <TextBlock Text="عرض الملصق الواحد (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding LabelWidth, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 70" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5,0,0,10">
                            <TextBlock Text="ارتفاع الملصق الواحد (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding LabelHeight, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 35" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>
                    </Grid>

                    <!-- 2x2 grid for margins -->
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Row 0: Left and Top Margins -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,5,10">
                            <TextBlock Text="الهامش الأيسر للورقة (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding MarginLeft, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 0" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="5,0,0,10">
                            <TextBlock Text="الهامش الأعلى للورقة (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding MarginTop, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 0" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>

                        <!-- Row 1: Right and Bottom Margins -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,5,10">
                            <TextBlock Text="الهامش الأيمن للورقة (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding MarginRight, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 0" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,0,0,10">
                            <TextBlock Text="الهامش الأسفل للورقة (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding MarginBottom, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 0" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,5,10">
                            <TextBlock Text="الفراغ الأفقي بين الملصقات (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding GapHorizontal, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 0" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5,0,0,10">
                            <TextBlock Text="الفراغ الرأسي بين الملصقات (مم)" Style="{StaticResource FieldLabelStyle}" />
                            <TextBox Text="{Binding GapVertical, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="مثال: 0" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="45" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,15,0,0">
            <Button Command="{Binding SaveCommand}" Content="حفظ القالب" Style="{StaticResource MaterialDesignRaisedButton}" Background="#1A3A6B" BorderBrush="#1A3A6B" Width="120" Height="38" Margin="0,0,10,0" FontFamily="{StaticResource CairoFont}" />
            <Button Click="Cancel_Click" Content="إلغاء" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#707070" BorderBrush="#E0E0E0" Width="100" Height="38" FontFamily="{StaticResource CairoFont}" />
        </StackPanel>
    </Grid>
</Window>

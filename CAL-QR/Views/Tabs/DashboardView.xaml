<UserControl x:Class="CAL_QR.Views.Tabs.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:local="clr-namespace:CAL_QR.Views.Tabs"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="Transparent">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1.2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Row 0: Header Info -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="لوحة البيانات والتحليل العام" FontSize="18" FontWeight="Bold" Foreground="#1A3A6B" FontFamily="{StaticResource CairoFont}" />
                    <TextBlock Text="نظرة شاملة على حالة الأجهزة المعايرة وتوزيع الجهات والتنبيهات النشطة" FontSize="11" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="حد التنبيه النشط: " FontSize="12" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                    <TextBlock Text="{Binding AlertThresholdDays}" FontSize="12" FontWeight="Bold" Foreground="#F9A825" Margin="0,0,5,0" />
                    <TextBlock Text="أيام" FontSize="12" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Row 1: Stat Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Card 1: Total Devices (Navy) -->
            <materialDesign:Card Grid.Column="0" Margin="0,0,5,0" Background="#1A3A6B" materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8" Height="75" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="إجمالي الأجهزة" FontSize="11" Foreground="#E0E0E0" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding TotalDevices}" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="{StaticResource CairoFont}" />
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="Database" Foreground="#80FFFFFF" Width="28" Height="28" VerticalAlignment="Center" />
                </Grid>
            </materialDesign:Card>

            <!-- Card 2: Valid Devices (Green) -->
            <materialDesign:Card Grid.Column="1" Margin="5,0,5,0" Background="#2E7D32" materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8" Height="75" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="سارية المفعول" FontSize="11" Foreground="#E8F5E9" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding ValidDevices}" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="{StaticResource CairoFont}" />
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="CheckCircle" Foreground="#80FFFFFF" Width="28" Height="28" VerticalAlignment="Center" />
                </Grid>
            </materialDesign:Card>

            <!-- Card 3: Expiring Soon (Yellow) -->
            <materialDesign:Card Grid.Column="2" Margin="5,0,5,0" Background="#F9A825" materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8" Height="75" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="تنتهي قريباً" FontSize="11" Foreground="#FFFDE7" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding ExpiringSoonDevices}" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="{StaticResource CairoFont}" />
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="AlertCircle" Foreground="#80FFFFFF" Width="28" Height="28" VerticalAlignment="Center" />
                </Grid>
            </materialDesign:Card>

            <!-- Card 4: Expired (Red) -->
            <materialDesign:Card Grid.Column="3" Margin="5,0,0,0" Background="#C62828" materialDesign:ElevationAssist.Elevation="Dp1" UniformCornerRadius="8" Height="75" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="منتهية الصلاحية" FontSize="11" Foreground="#FFEBEE" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding ExpiredDevices}" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="{StaticResource CairoFont}" />
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="AlertDecagram" Foreground="#80FFFFFF" Width="28" Height="28" VerticalAlignment="Center" />
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- Row 2: Charts -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Donut Chart -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" Margin="0,0,5,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="توزيع الأجهزة حسب الجهات المالكة" FontSize="12" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,10" FontFamily="{StaticResource CairoFont}" />
                    <lvc:PieChart Grid.Row="1" Series="{Binding PieSeriesCollection}" LegendPosition="Right" />
                </Grid>
            </Border>

            <!-- Right: Column Chart -->
            <Border Grid.Column="1" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" Margin="5,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="مقارنة حالات الصلاحية" FontSize="12" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,10" FontFamily="{StaticResource CairoFont}" />
                    <lvc:CartesianChart Grid.Row="1" Series="{Binding BarSeriesCollection}" XAxes="{Binding XAxes}" />
                </Grid>
            </Border>
        </Grid>

        <!-- Row 3: Expiring Soon List -->
        <Border Grid.Row="3" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="الأجهزة التي ستنتهي صلاحية معايرتها قريباً" FontSize="12" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,10" FontFamily="{StaticResource CairoFont}" />

                <DataGrid Grid.Row="1" ItemsSource="{Binding ExpiringDevices}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" RowHeaderWidth="0" GridLinesVisibility="Horizontal">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="الموديل / الجهاز" Binding="{Binding DeviceModel}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate><TextBlock Text="الموديل / الجهاز" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="الرقم التسلسلي" Binding="{Binding SerialNumber}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate><TextBlock Text="الرقم التسلسلي" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="الجهة المالكة" Binding="{Binding OwnerName}" Width="*" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate><TextBlock Text="الجهة المالكة" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="تاريخ الانتهاء" Binding="{Binding ExpiryDateString}" Width="120" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                            <DataGridTextColumn.HeaderTemplate>
                                <DataTemplate><TextBlock Text="تاريخ الانتهاء" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                            </DataGridTextColumn.HeaderTemplate>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="الأيام المتبقية" Width="120">
                            <DataGridTemplateColumn.HeaderTemplate>
                                <DataTemplate><TextBlock Text="الأيام المتبقية" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                            </DataGridTemplateColumn.HeaderTemplate>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="ClockAlert" Foreground="#F9A825" Margin="0,0,5,0" Width="14" Height="14" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding DaysRemaining}" FontWeight="Bold" Foreground="#F9A825" VerticalAlignment="Center" />
                                        <TextBlock Text=" يوم متبقي" Foreground="#707070" FontSize="11" Margin="3,0,0,0" VerticalAlignment="Center" FontFamily="{StaticResource CairoFont}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>

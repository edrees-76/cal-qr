<UserControl x:Class="CAL_QR.Views.Tabs.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:local="clr-namespace:CAL_QR.Views.Tabs"
             xmlns:helpers="clr-namespace:CAL_QR.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <helpers:DoubleToGridLengthConverter x:Key="DoubleToGridLengthConverter" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1.2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Row 0: Header Info -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="لوحة البيانات والتحليل العام" FontSize="18" FontWeight="Bold" Foreground="#1A3A6B" FontFamily="{StaticResource CairoFont}" />
                    <TextBlock Text="نظرة شاملة على حالة الأجهزة المعايرة وتوزيع الجهات والتنبيهات النشطة" FontSize="11" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="حد التنبيه النشط: " FontSize="12" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                    <TextBlock Text="{Binding AlertThresholdDays}" FontSize="12" FontWeight="Bold" Foreground="#F9A825" Margin="0,0,5,0" />
                    <TextBlock Text="أيام" FontSize="12" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Row 1: Stat Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Card 1: Total Devices (Navy) -->
            <Border Grid.Column="0" Margin="0,0,5,0" Background="#1A3A6B" CornerRadius="8" Padding="12" Height="85">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="إجمالي الأجهزة" FontSize="12" Foreground="#E0E0E0" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding TotalDevices}" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="{StaticResource CairoFont}" />
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="Database" Foreground="#80FFFFFF" Width="28" Height="28" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- Card 2: Valid Devices (Green) -->
            <Border Grid.Column="1" Margin="5,0,5,0" Background="#2E7D32" CornerRadius="8" Padding="12" Height="85">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="سارية المفعول" FontSize="12" Foreground="#E8F5E9" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding ValidDevices}" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="{StaticResource CairoFont}" />
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="CheckCircle" Foreground="#80FFFFFF" Width="28" Height="28" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- Card 3: Expiring Soon (Yellow) -->
            <Border Grid.Column="2" Margin="5,0,5,0" Background="#F9A825" CornerRadius="8" Padding="12" Height="85">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="تنتهي قريباً" FontSize="12" Foreground="#FFFDE7" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding ExpiringSoonDevices}" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="{StaticResource CairoFont}" />
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="AlertCircle" Foreground="#80FFFFFF" Width="28" Height="28" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- Card 4: Expired (Red) -->
            <Border Grid.Column="3" Margin="5,0,0,0" Background="#C62828" CornerRadius="8" Padding="12" Height="85">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="منتهية الصلاحية" FontSize="12" Foreground="#FFEBEE" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding ExpiredDevices}" FontSize="24" FontWeight="Bold" Foreground="White" FontFamily="{StaticResource CairoFont}" />
                    </StackPanel>
                    <materialDesign:PackIcon Grid.Column="1" Kind="AlertDecagram" Foreground="#80FFFFFF" Width="28" Height="28" VerticalAlignment="Center" />
                </Grid>
            </Border>
        </Grid>

        <!-- Row 2: Charts -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <!-- Left: Owner Distribution Flat Bar Chart (Expanded to full width) -->
            <Border Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" Margin="0" FlowDirection="RightToLeft">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="توزيع الأجهزة حسب الجهات المالكة" FontSize="12" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,10" FontFamily="{StaticResource CairoFont}" />
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="220">
                        <ItemsControl ItemsSource="{Binding OwnerDeviceStats}" VerticalAlignment="Top">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,2,0,2">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.1*" />
                                            <ColumnDefinition Width="2.4*" />
                                            <ColumnDefinition Width="35" />
                                        </Grid.ColumnDefinitions>

                                        <Grid Grid.Column="0" VerticalAlignment="Center">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="6" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Rank -->
                                            <TextBlock Grid.Column="0" Text="{Binding Rank, StringFormat={}{0}.}" FontWeight="Bold" Foreground="#1A3A6B" FontSize="10.5" FontFamily="{StaticResource CairoFont}" VerticalAlignment="Top" />
                                            
                                            <!-- Owner Name -->
                                            <TextBlock Grid.Column="2" Text="{Binding OwnerName}" FontSize="10.5" Foreground="#424242" FontFamily="{StaticResource CairoFont}" TextWrapping="Wrap" HorizontalAlignment="Stretch" VerticalAlignment="Top" />
                                        </Grid>
                                        
                                        <!-- Proportional Horizontal Bar -->
                                        <Grid Grid.Column="1" Height="10" Margin="10,0,10,0" VerticalAlignment="Center">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="{Binding BarWidthPercentage, Converter={StaticResource DoubleToGridLengthConverter}}" />
                                                <ColumnDefinition Width="{Binding BarWidthPercentage, Converter={StaticResource DoubleToGridLengthConverter}, ConverterParameter=Remaining}" />
                                            </Grid.ColumnDefinitions>
                                            
                                            <!-- Gray Background Bar -->
                                            <Border Grid.ColumnSpan="2" Background="#EEEEEE" CornerRadius="5" />
                                            
                                            <!-- Proportional Colored Bar with Gradient -->
                                            <Border Grid.Column="0" CornerRadius="5">
                                                <Border.Background>
                                                    <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                        <GradientStop Color="#1A3A6B" Offset="0.0" />
                                                        <GradientStop Color="#C9A227" Offset="1.0" />
                                                    </LinearGradientBrush>
                                                </Border.Background>
                                            </Border>
                                        </Grid>
                                        
                                        <!-- Count -->
                                        <TextBlock Grid.Column="2" Text="{Binding Count}" FontSize="10.5" FontWeight="Bold" Foreground="#1A3A6B" FontFamily="{StaticResource CairoFont}" HorizontalAlignment="Left" VerticalAlignment="Center" />
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Row 3: Lists Section -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Card 1: Expiring Soon List (Right in RTL) -->
            <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="الأجهزة التي ستنتهي صلاحية معايرتها قريباً" FontSize="12" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,10" FontFamily="{StaticResource CairoFont}" />

                    <DataGrid Grid.Row="1" ItemsSource="{Binding ExpiringDevices}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" RowHeaderWidth="0" GridLinesVisibility="Horizontal">
                        <DataGrid.Resources>
                            <Style x:Key="WrapTextColumnStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
                                <Setter Property="TextWrapping" Value="Wrap" />
                            </Style>
                            <Style x:Key="CenterTextColumnStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                <Setter Property="TextAlignment" Value="Center" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource UnifiedDataGridRowHoverStyle}">
                                <Setter Property="ToolTip" Value="انقر نقراً مزدوجاً لعرض تفاصيل الجهاز الكاملة" />
                                <Setter Property="Cursor" Value="Hand" />
                                <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick" />
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ت" Binding="{Binding Rank}" Width="45" CanUserSort="False" ElementStyle="{StaticResource CenterTextColumnStyle}">
                                <DataGridTextColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="ت" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" HorizontalAlignment="Center" /></DataTemplate>
                                </DataGridTextColumn.HeaderTemplate>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="الجهاز / الرقم التسلسلي" Width="1.4*" MinWidth="110">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="الجهاز / الرقم التسلسلي" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical" Margin="0,3">
                                            <TextBlock Text="{Binding DeviceModel}" FontWeight="Bold" FontSize="11" TextWrapping="Wrap" ToolTip="{Binding DeviceModel}" />
                                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                <TextBlock Text="الرقم: " Foreground="#888888" FontSize="10" FontFamily="{StaticResource CairoFont}" />
                                                <TextBlock Text="{Binding SerialNumber}" Foreground="#555555" FontSize="10" FontWeight="SemiBold" ToolTip="{Binding SerialNumber}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="الجهة المالكة" Binding="{Binding OwnerName}" Width="1.3*" MinWidth="100" ElementStyle="{StaticResource WrapTextColumnStyle}">
                                <DataGridTextColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="الجهة المالكة" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                                </DataGridTextColumn.HeaderTemplate>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="تاريخ الانتهاء" Binding="{Binding ExpiryDateString}" Width="1*" MinWidth="85" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                <DataGridTextColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="تاريخ الانتهاء" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                                </DataGridTextColumn.HeaderTemplate>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="الأيام المتبقية" Width="1.1*" MinWidth="95">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="الأيام المتبقية" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <materialDesign:PackIcon Kind="ClockAlert" Foreground="#F9A825" Margin="0,0,5,0" Width="14" Height="14" VerticalAlignment="Center" />
                                            <TextBlock Text="{Binding DaysRemaining}" FontWeight="Bold" Foreground="#F9A825" VerticalAlignment="Center" />
                                            <TextBlock Text=" يوم" Foreground="#707070" FontSize="11" Margin="3,0,0,0" VerticalAlignment="Center" FontFamily="{StaticResource CairoFont}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Card 2: Expired List (Left in RTL) -->
            <Border Grid.Column="1" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="الأجهزة منتهية الصلاحية فعلياً" FontSize="12" FontWeight="Bold" Foreground="#C62828" Margin="0,0,0,10" FontFamily="{StaticResource CairoFont}" />

                    <DataGrid Grid.Row="1" ItemsSource="{Binding ExpiredDevicesList}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Background="Transparent" BorderThickness="0" RowHeaderWidth="0" GridLinesVisibility="Horizontal">
                        <DataGrid.Resources>
                            <Style x:Key="WrapTextColumnStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                <Setter Property="ToolTip" Value="{Binding RelativeSource={RelativeSource Self}, Path=Text}" />
                                <Setter Property="TextWrapping" Value="Wrap" />
                            </Style>
                            <Style x:Key="CenterTextColumnStyle" TargetType="TextBlock" BasedOn="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                <Setter Property="TextAlignment" Value="Center" />
                                <Setter Property="FontWeight" Value="Bold" />
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource UnifiedDataGridRowHoverStyle}">
                                <Setter Property="ToolTip" Value="انقر نقراً مزدوجاً لعرض تفاصيل الجهاز الكاملة" />
                                <Setter Property="Cursor" Value="Hand" />
                                <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick" />
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ت" Binding="{Binding Rank}" Width="45" CanUserSort="False" ElementStyle="{StaticResource CenterTextColumnStyle}">
                                <DataGridTextColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="ت" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" HorizontalAlignment="Center" /></DataTemplate>
                                </DataGridTextColumn.HeaderTemplate>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="الجهاز / الرقم التسلسلي" Width="1.4*" MinWidth="110">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="الجهاز / الرقم التسلسلي" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Vertical" Margin="0,3">
                                            <TextBlock Text="{Binding DeviceModel}" FontWeight="Bold" FontSize="11" TextWrapping="Wrap" ToolTip="{Binding DeviceModel}" />
                                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                <TextBlock Text="الرقم: " Foreground="#888888" FontSize="10" FontFamily="{StaticResource CairoFont}" />
                                                <TextBlock Text="{Binding SerialNumber}" Foreground="#555555" FontSize="10" FontWeight="SemiBold" ToolTip="{Binding SerialNumber}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="الجهة المالكة" Binding="{Binding OwnerName}" Width="1.3*" MinWidth="100" ElementStyle="{StaticResource WrapTextColumnStyle}">
                                <DataGridTextColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="الجهة المالكة" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                                </DataGridTextColumn.HeaderTemplate>
                            </DataGridTextColumn>

                            <DataGridTextColumn Header="تاريخ الانتهاء" Binding="{Binding ExpiryDateString}" Width="1*" MinWidth="85" ElementStyle="{StaticResource MaterialDesignDataGridTextColumnStyle}">
                                <DataGridTextColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="تاريخ الانتهاء" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                                </DataGridTextColumn.HeaderTemplate>
                            </DataGridTextColumn>

                            <DataGridTemplateColumn Header="الحالة" Width="1.1*" MinWidth="95">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate><TextBlock Text="الحالة" FontWeight="Bold" FontFamily="{StaticResource CairoFont}" /></DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <materialDesign:PackIcon Kind="AlertDecagram" Foreground="#C62828" Margin="0,0,5,0" Width="14" Height="14" VerticalAlignment="Center" />
                                            <TextBlock Text="{Binding DaysExpiredText}" FontWeight="Bold" Foreground="#C62828" VerticalAlignment="Center" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

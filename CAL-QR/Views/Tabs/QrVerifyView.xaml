<UserControl x:Class="CAL_QR.Views.Tabs.QrVerifyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:CAL_QR.Views.Tabs"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="التحقق الرقمي من صحة شهادة المعايرة" FontSize="18" FontWeight="Bold" Foreground="#1A3A6B" FontFamily="{StaticResource CairoFont}" />
                <TextBlock Text="تحليل محتويات كود QR ومطابقة التوقيع الإلكتروني المشفر لمنع التزوير" FontSize="11" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">
            <TabControl.Resources>
                <Style TargetType="TabItem" BasedOn="{StaticResource MaterialDesignNavigationRailTabItem}">
                    <Setter Property="FontFamily" Value="{StaticResource CairoFont}" />
                    <Setter Property="FontSize" Value="13" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="Padding" Value="15,10" />
                </Style>
            </TabControl.Resources>

            <!-- Paste Tab -->
            <TabItem Header="التحقق عبر لصق نص QR">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="1.2*" />
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" Margin="0,0,10,0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="قم بلصق النص المقروء من كاميرا QR هنا:" FontSize="12" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,10" FontFamily="{StaticResource CairoFont}" />
                            <TextBox Grid.Row="1" Text="{Binding ConcatenatedText, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="NaN" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" VerticalScrollBarVisibility="Auto" FontFamily="{StaticResource CairoFont}" />
                            
                            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,15,0,0">
                                <Button Command="{Binding VerifyPastedTextCommand}" Content="تحليل وتحقق" Style="{StaticResource MaterialDesignRaisedButton}" Background="#1A3A6B" BorderBrush="#1A3A6B" Height="36" Margin="0,0,10,0" FontFamily="{StaticResource CairoFont}" />
                                <Button Command="{Binding ClearCommand}" Content="مسح" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#707070" BorderBrush="#E0E0E0" Height="36" FontFamily="{StaticResource CairoFont}" />
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Parsed Fields View -->
                    <Border Grid.Column="1" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="البيانات المستخرجة من الـ QR:" FontSize="12" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,10" FontFamily="{StaticResource CairoFont}" />
                            
                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <!-- Owner -->
                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="الجهة المالكة:" FontWeight="SemiBold" Margin="5,8" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Owner}" FontWeight="Bold" Margin="10,8" Foreground="#212121" FontFamily="{StaticResource CairoFont}" />

                                    <!-- Cert Number -->
                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="رقم الشهادة:" FontWeight="SemiBold" Margin="5,8" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding CertNo}" FontWeight="Bold" Margin="10,8" Foreground="#212121" FontFamily="{StaticResource CairoFont}" />

                                    <!-- Device Type -->
                                    <TextBlock Grid.Row="2" Grid.Column="0" Text="نوع الجهاز:" FontWeight="SemiBold" Margin="5,8" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding DeviceType}" FontWeight="Bold" Margin="10,8" Foreground="#212121" FontFamily="{StaticResource CairoFont}" />

                                    <!-- Model / Serial -->
                                    <TextBlock Grid.Row="3" Grid.Column="0" Text="الموديل / الرقم التسلسلي:" FontWeight="SemiBold" Margin="5,8" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                    <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" Margin="10,8">
                                        <TextBlock Text="{Binding Model}" FontWeight="Bold" Foreground="#212121" FontFamily="{StaticResource CairoFont}" />
                                        <TextBlock Text=" - " Foreground="#707070" />
                                        <TextBlock Text="{Binding Serial}" FontWeight="Bold" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                    </StackPanel>

                                    <!-- Dates -->
                                    <TextBlock Grid.Row="4" Grid.Column="0" Text="تاريخ المعايرة / الانتهاء:" FontWeight="SemiBold" Margin="5,8" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="10,8">
                                        <TextBlock Text="{Binding CalDate}" FontWeight="Bold" Foreground="#212121" FontFamily="{StaticResource CairoFont}" />
                                        <TextBlock Text="  إلى  " Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                        <TextBlock Text="{Binding ExpDate}" FontWeight="Bold" Foreground="#212121" FontFamily="{StaticResource CairoFont}" />
                                    </StackPanel>

                                    <!-- Engineer -->
                                    <TextBlock Grid.Row="5" Grid.Column="0" Text="المهندس المعايِر:" FontWeight="SemiBold" Margin="5,8" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                    <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Engineer}" FontWeight="Bold" Margin="10,8" Foreground="#212121" FontFamily="{StaticResource CairoFont}" />

                                    <!-- Result -->
                                    <TextBlock Grid.Row="6" Grid.Column="0" Text="النتيجة:" FontWeight="SemiBold" Margin="5,8" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                    <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding Result}" FontWeight="Bold" Margin="10,8" Foreground="#212121" FontFamily="{StaticResource CairoFont}" />

                                    <!-- Code -->
                                    <TextBlock Grid.Row="7" Grid.Column="0" Text="كود التحقق المقروء:" FontWeight="SemiBold" Margin="5,8" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                                    <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding ReadVerifyCode}" FontWeight="Bold" Margin="10,8" Foreground="#C62828" FontFamily="{StaticResource CairoFont}" />
                                </Grid>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Manual Tab -->
            <TabItem Header="التحقق بالإدخال اليدوي للبيانات">
                <Border Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1" Padding="15" Margin="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="أدخل الحقول المكتوبة على الشهادة لمطابقتها:" FontSize="12" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,15" FontFamily="{StaticResource CairoFont}" />

                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                <!-- Row 0 -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
                                    <TextBlock Text="اسم الجهة المالكة" FontSize="11" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,5" FontFamily="{StaticResource CairoFont}" />
                                    <TextBox Text="{Binding ManualOwner, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="40" FontFamily="{StaticResource CairoFont}" />
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
                                    <TextBlock Text="الموديل / الطراز" FontSize="11" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,5" FontFamily="{StaticResource CairoFont}" />
                                    <TextBox Text="{Binding ManualModel, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="40" FontFamily="{StaticResource CairoFont}" />
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="2" Margin="5">
                                    <TextBlock Text="الرقم التسلسلي (S/N)" FontSize="11" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,5" FontFamily="{StaticResource CairoFont}" />
                                    <TextBox Text="{Binding ManualSerial, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="40" FontFamily="{StaticResource CairoFont}" />
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="3" Margin="5">
                                    <TextBlock Text="رقم الشهادة" FontSize="11" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,5" FontFamily="{StaticResource CairoFont}" />
                                    <TextBox Text="{Binding ManualCertNo, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="40" FontFamily="{StaticResource CairoFont}" />
                                </StackPanel>

                                <!-- Row 1 -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
                                    <TextBlock Text="تاريخ المعايرة" FontSize="11" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,5" FontFamily="{StaticResource CairoFont}" />
                                    <DatePicker SelectedDate="{Binding ManualCalDate}" Style="{StaticResource MaterialDesignOutlinedDatePicker}" Height="40" FontFamily="{StaticResource CairoFont}" />
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
                                    <TextBlock Text="تاريخ الانتهاء" FontSize="11" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,5" FontFamily="{StaticResource CairoFont}" />
                                    <DatePicker SelectedDate="{Binding ManualExpDate}" Style="{StaticResource MaterialDesignOutlinedDatePicker}" Height="40" FontFamily="{StaticResource CairoFont}" />
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="2" Margin="5">
                                    <TextBlock Text="المهندس المعايِر" FontSize="11" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,5" FontFamily="{StaticResource CairoFont}" />
                                    <TextBox Text="{Binding ManualEngineer, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="40" FontFamily="{StaticResource CairoFont}" />
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="3" Margin="5">
                                    <TextBlock Text="النتيجة" FontSize="11" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,5" FontFamily="{StaticResource CairoFont}" />
                                    <ComboBox SelectedValue="{Binding ManualResult}" SelectedValuePath="Tag" Style="{StaticResource MaterialDesignOutlinedComboBox}" Height="40" FontFamily="{StaticResource CairoFont}">
                                        <ComboBoxItem Content="✅ ناجح" Tag="Passed" />
                                        <ComboBoxItem Content="❌ راسب" Tag="Failed" />
                                        <ComboBoxItem Content="⚠️ مشروط" Tag="Conditional" />
                                    </ComboBox>
                                </StackPanel>

                                <!-- Row 2 -->
                                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="5">
                                    <TextBlock Text="كود التحقق المكتوب بالشهادة (Verify Code)" FontSize="11" FontWeight="Bold" Foreground="#1A3A6B" Margin="0,0,0,5" FontFamily="{StaticResource CairoFont}" />
                                    <TextBox Text="{Binding ManualVerifyCode, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="40" FontFamily="{StaticResource CairoFont}" />
                                </StackPanel>
                            </Grid>
                        </ScrollViewer>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,15,0,0">
                            <Button Command="{Binding VerifyManualCommand}" Content="تحقق يدوياً" Style="{StaticResource MaterialDesignRaisedButton}" Background="#1A3A6B" BorderBrush="#1A3A6B" Height="36" Margin="0,0,10,0" FontFamily="{StaticResource CairoFont}" />
                            <Button Command="{Binding ClearCommand}" Content="مسح" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#707070" BorderBrush="#E0E0E0" Height="36" FontFamily="{StaticResource CairoFont}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </TabItem>
        </TabControl>

        <!-- Verification Status Banner -->
        <Border Grid.Row="2" CornerRadius="8" Padding="15" Margin="0,10,0,0" Visibility="{Binding IsValidated, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#FFEBEE" />
                    <Setter Property="BorderBrush" Value="#EF9A9A" />
                    <Setter Property="BorderThickness" Value="1.5" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                            <Setter Property="Background" Value="#E8F5E9" />
                            <Setter Property="BorderBrush" Value="#A5D6A7" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0" Margin="0,0,15,0" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="CheckCircle" Foreground="#2E7D32" Width="30" Height="30" Visibility="{Binding IsSuccess, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    <materialDesign:PackIcon Kind="AlertDecagram" Foreground="#C62828" Width="30" Height="30">
                        <materialDesign:PackIcon.Style>
                            <Style TargetType="materialDesign:PackIcon" BasedOn="{StaticResource {x:Type materialDesign:PackIcon}}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </materialDesign:PackIcon.Style>
                    </materialDesign:PackIcon>
                </Grid>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Message}" FontSize="15" FontWeight="Bold" FontFamily="{StaticResource CairoFont}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#C62828" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                        <Setter Property="Foreground" Value="#2E7D32" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="الكود المقروء بالشهادة: " FontSize="11" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding ReadVerifyCode}" FontSize="11" FontWeight="Bold" Foreground="#C62828" Margin="0,0,15,0" />
                        <TextBlock Text="الكود الفعلي المولد: " FontSize="11" Foreground="#707070" FontFamily="{StaticResource CairoFont}" />
                        <TextBlock Text="{Binding ComputedVerifyCode}" FontSize="11" FontWeight="Bold" Foreground="#2E7D32" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

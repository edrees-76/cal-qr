# ملف مرجعي — القرارات المتفق عليها بخصوص ميزة "إنشاء الشهادة الكاملة" في CAL-QR

**الإصدار:** 2.0
**تاريخ التحديث:** 2026-07-27
**سبب التحديث:** وصول أول نموذجين حقيقيين من المهندس رضا (`B105` — Pancake GM 44-9، و`G501` — Gamma Scintillation Probe 44-2)، وحسم خمس نقاط تصميمية كانت معلّقة.

> **ملاحظة على الإصدار:** هذا الإصدار يُلغي ويستبدل الإصدار 1.0. الأقسام المعلّمة بـ 🆕 جديدة، والمعلّمة بـ ⚠️ **مُصحّحة** عن الإصدار السابق بناءً على النماذج الفعلية.

---

## القسم 1: نطاق الميزة والمبدأ الأساسي

1. كل سجل معايرة (`CalibrationRecord`) يتطلب شهادة كاملة — **لا توجد سجلات مختصرة (Lightweight Records)**.
2. زر **"إنشاء شهادة"** يظهر داخل نافذة سجل المعايرة القائمة، ويفتح نافذة فرعية (Sub-Dialog) تحتوي على الحقول الخاصة بنوع الجهاز.
3. الشهادة الموقّعة والممسوحة ضوئياً تُرفق عبر ميزة المرفقات القائمة (اختياري).
4. الكيان الذي تصدر له الشهادة هو دائماً **الكاشف/المجس (Probe/Detector)**. جهاز القراءة (Survey Meter) يظهر كسياق قياس فقط.

---

## القسم 2: ترقيم الشهادات

1. الصيغة: `TNRC-SSDL-YYYY-XXXX`
2. تصفير التسلسل سنوياً مع بداية كل سنة ميلادية.
3. عدّاد **مشترك** بين جميع أنواع الأجهزة (وليس عدّاداً لكل نوع).
4. **الالتزام المتأخر (Late Commit):** الرقم لا يُحجز مبكراً إطلاقاً؛ يُولّد ويُثبّت في قاعدة البيانات **فقط عند الحفظ النهائي** للنافذة الفرعية. الأرقام الملغاة تعود للمخزون تلقائياً.

### 🆕 2.5 الرقم الإشاري (Reference No)
- حقل **منفصل تماماً** عن رقم الشهادة، ظهر في أعلى النموذجين.
- **يُدخل يدوياً** بواسطة المستخدم (رقم وارد/صادر إداري).
- نصّي حر، **اختياري**، لا يُولّد تلقائياً، ولا يخضع لأي تسلسل أو تحقق.

---

## القسم 3: المبدأ المعماري الحاكم 🆕

> **الشهادة الصادرة وثيقة تاريخية مجمّدة (Immutable Historical Snapshot).**

كل بيانات الشهادة تُنسخ **نصياً** داخل سجل الشهادة وقت الإصدار، ولا تُربط بمرجع خارجي حي (`Foreign Key` إلى `Users` أو `AppSettings` أو `Owners`).

**السبب:** تغيير رئيس القسم أو اسم/عنوان الجهة بعد سنة يجب ألا يُغيّر بأثر رجعي محتوى شهادات صدرت وطُبعت ووُقّعت فعلياً. الربط الحي بمرجع خارجي يُنتج تزويراً غير مقصود للسجلات ويخالف متطلبات ISO/IEC 17025:2017 بخصوص سلامة السجلات.

**ينطبق على:** أسماء الموقّعين الأربعة وألقابهم، اسم العميل، عنوان العميل، موديل الجهاز ورقمه التسلسلي، بيانات جهاز القراءة.

**استثناء:** تبقى `DeviceId` و`CalibrationRecordId` كمفاتيح ربط للتنقّل والاستعلام فقط — لا لعرض البيانات.

---

## القسم 4: بنية البيانات — مكتبة الحقول المرنة ⚠️ (محسوم نهائياً)

النموذجان الواردان متباعدان جوهرياً (أحدهما بجدول عدم يقين من 5 مكوّنات، والآخر **بلا قسم عدم يقين إطلاقاً**؛ أحدهما بجدول نتائج متعدد الصفوف، والآخر بقيمة واحدة). هذا يُلغي نهائياً خيار **الأعمدة الثابتة (Fixed Columns)** ويحسم الأمر لصالح:

### 4.1 الكيان الرئيسي: `Certificate`
يحمل الحقول المشتركة المؤكدة في كل النماذج + `CalibrationRecordId` + الحقول المنسوخة نصياً (القسم 3).

### 4.2 الجداول الأبناء الديناميكية (Child Tables)
| الجدول | الغرض | ملاحظة |
|---|---|---|
| `CertificateCalibrationResult` | صفوف نتائج المعايرة | عدد الصفوف متغيّر (3 في Pancake، صف واحد في Gamma) |
| `CertificateUncertaintyComponent` | مكوّنات ميزانية عدم اليقين | ديناميكي بالكامل؛ **القسم كله قابل للإخفاء** |
| `CertificateFunctionalCheck` | الفحوصات الوظيفية | ديناميكي؛ قائمة افتراضية تُقترح حسب نوع الجهاز |

### 4.3 قابلية إظهار/إخفاء الأقسام
الإظهار والإخفاء يكون **على مستوى القسم بأكمله**، وليس على مستوى الحقول الفردية فقط — لأن قسمي "عدم اليقين" و"منهجية المعايرة" غائبان كلياً في بعض الأنواع.

---

## القسم 5: الأقسام المنطقية للشهادة ⚠️ (محدّث من 6 إلى 8 أقسام)

| # | القسم | الحالة | ملاحظات |
|---|---|---|---|
| 1 | الترويسة (Header) | **ثابت دائماً** | الشعارات + دولة ليبيا / مؤسسة الطاقة الذرية / TNRC / SSDL / وحدة المعايرة — متطابق حرفياً في النموذجين |
| 2 | بيانات العميل والجهاز | **ثابت دائماً** | + 🆕 `Client Address` + 🆕 `Manufacturer` (ينتمي منطقياً لكيان `Device` لا للشهادة) |
| 3 | الظروف البيئية | **ثابت دائماً** | حرارة / رطوبة نسبية / ضغط جوي — متطابق حرفياً 100% |
| 4 | نتائج المعايرة | **متغيّر جذرياً** | جدول متعدد الصفوف أو قيمة واحدة (CF + AE%) |
| 5 | ميزانية عدم اليقين | **اختياري (قابل للإخفاء كلياً)** | غائب تماماً في Gamma Probe |
| 6 | 🆕 منهجية المعايرة | **اختياري** | ظهر في G501 فقط: مصدر الإشعاع، الهندسة المرجعية، فقرة منهجية نصية، مرجع التتبعية |
| 7 | الفحوصات الوظيفية | **متغيّر (يعدّله المستخدم)** | الهيكل ثابت (رقم/الفحص/المتطلب/النتيجة)، المحتوى قابل للتعديل والإضافة والحذف |
| 8 | الاعتماد والتوقيعات | **ثابت الهيكل، متغيّر المحتوى** | انظر القسم 6 |

**التذييل:** عبارة ISO/IEC 17025:2017 ثابتة حرفياً + قسم "الملاحظات والشروط الهامة" (نصوص قابلة للتعديل).

---

## القسم 6: خانات التوقيع ⚠️ **تصحيح جوهري**

النماذج الفعلية تحتوي على **4 خانات** وليس 3 كما افترضنا سابقاً:

| # | الخانة | اللقب الافتراضي |
|---|---|---|
| 1 | CALIBRATED BY | SSDL - TNRC |
| 2 | REVIEWED BY | Calibration Unit Head — SSDL - TNRC |
| 3 | APPROVED BY | Head of Department — SSDL - TNRC |
| 4 | AUTHORIZED BY | Radiation Protection — TNRC Management |

### 🆕 نمط "الافتراضي القابل للتجاوز" (Default-with-Override)
- الأسماء والألقاب الافتراضية تُحفظ **مرة واحدة** في `AppSettings`.
- تُملأ **تلقائياً** في كل شهادة جديدة.
- **يستطيع المستخدم تعديلها داخل تلك الشهادة تحديداً** (لوجود مكلّف بديل عند غياب أحدهم) دون تغيير القيمة الافتراضية العامة.
- القيمة المعدّلة تُحفظ نصياً داخل سجل الشهادة (تطبيقاً لمبدأ القسم 3).
- لكل خانة حقل تاريخ مستقل.

---

## القسم 7: تفاصيل مستخرجة من النموذجين

### 7.1 جدول ميزانية عدم اليقين ⚠️ **تصحيح**
الأعمدة الفعلية **ستة** وليس ثلاثة كما وُثّق سابقاً:

`No. | Component | Evaluation Type (A/B) | Standard Uncertainty (%) | Contribution (%) | Distribution`

- **Distribution** قيم مشاهدة: Normal / Poisson / Rectangular
- صفّا مجموع في الأسفل: **Combined Standard Uncertainty (uc)** و**Expanded Uncertainty (U) (k=2)**
- مثال من B105: 5 مكوّنات، uc = 5.40%، U = 10.80%

### 7.2 الفروقات المؤكدة بين النوعين

| البند | B105 — Pancake GM 44-9 | G501 — Gamma Scintillation 44-2 |
|---|---|---|
| نتائج المعايرة | جدول 3 مصادر (Source ID، Radionuclide، Scale، Reference Value kCPM، Measured Reading kCPM، CF) | قيمة واحدة: CF + Absolute Relative Error (%) |
| المتوسط | Average Correction Factor (CFavg = ΣCF / n) | — |
| عدم اليقين | جدول كامل (5 مكوّنات) | **غائب تماماً** |
| منهجية المعايرة | **غائب** | قسم كامل (Co-60/Cs-137، مسافة 1.0 م، Farmer chamber، IAEA) |
| معلومات تقنية | Geometry: Contact، Counting Time: 60 Sec، Counting Unit: kCPM، Radiation Traceability | — |
| الحكم النهائي | STATUS / VERDICT | Final Compliance Verdict |
| فحص وظيفي مميّز | Detector Window Inspection | Detector Response Check |
| Manufacturer | غائب | موجود |
| معلومات إضافية | — | قسم Additional Information (نقاط حرة) |
| صيغة الحساب | Corrected Reading = Measured × CFavg | Corrected Reading = Measured × CF |

---

## القسم 8: أنواع الأجهزة الخمسة

تُزرع كسجلات `DeviceType` افتراضية في `DatabaseMigrator.cs`:
1. Pancake Probe ✅ (نموذج وارد: B105)
2. Gamma Probe (NaI Scintillator) ✅ (نموذج وارد: G501)
3. Beta Scintillator Probe ⏳ (بانتظار النموذج)
4. PED ⏳ (بانتظار النموذج)
5. Dose Rate Meter ⏳ (بانتظار النموذج)

---

## القسم 9: كيان `SurveyMeter`

- كيان شبه مُدار، مرتبط بـ `Owner` بعلاقة (واحد إلى متعدد).
- يُعرض داخل `OwnerDetailDialog`.
- الإدخال بنفس نمط ComboBox/AutoComplete/الحفظ الذرّي المعتمد.
- قاعدة التفعيل: `!string.IsNullOrWhiteSpace(OwnerText?.Trim())`
- يظهر في الشهادة كسياق قياس: الموديل + الرقم التسلسلي (منسوخين نصياً).

---

## القسم 10: بنود مؤجلة ومعلّقة

### 🔴 10.0 تبعيات حاجزة — تُنفَّذ وإلا شُحنت ميزة معطّلة صامتة

| # | التبعية | المرحلة المالكة | أثر إغفالها |
|---|---|---|---|
| 1 | **أول طباعة للشهادة يجب أن تستدعي `ICertificateRepository.MarkPrintedAsync`** | مرحلة طباعة الشهادة | `FirstPrintedAt` يبقى `null` ← شرط `AmendedAt` لا يتحقق أبداً ← حالة ⚠ «عُدّلت هذه الشهادة» **لا تظهر إطلاقاً**. مسار الرموز التاريخية يعمل، لكن بلا وسم تعديل. لا توجد أي إشارة فشل. |
| 2 | **شاشة التحقق تستدعي `VerifyByCodeAsync` بمراحلها الثلاث** | مرحلة ربط الواجهة | `QrVerifyViewModel` الحالي يبحث في `CalibrationRecords` فقط. رمز شهادة قديم يُعيد «غير موجودة»، ورمز حالي لا يُتعرَّف عليه أصلاً. |
| 3 | **الملصق والشهادة يقرآن التواريخ من `Certificate` لا من `CalibrationRecord`** | مرحلة الملصق | النسخة المجمّدة هي المرجع؛ القراءة من السجل الحيّ تُباعد القيمتين بعد أول تعديل. |
| 4 | **حسم مصير `AverageCorrectionFactor` المفرد** أمام قاعدة `CF` لكل نويدة | مرحلة الملصق | لا يفتح ثغرة توقيع (القيم التفصيلية داخل صفوف `R` محمية)، لكن **إلغاءه لاحقاً يحتاج `V2`** بينما بقاؤه مع تجاهله لا يحتاج شيئاً. |
| 5 | **إبطال شهادة صادرة (`SoftDelete`)** — يُنفَّذ عند وجود مستدعٍ حقيقي، بفحص تصريح في **طبقة التطبيق** بعلم `SystemPermissions` مخصص. **لا داخل المستودع، ولا بـ`UserRole`.** | مرحلة إدارة الشهادات | المخطط يدعمها فعلاً (الفهرس الفريد المشروط على `CalibrationRecordId` يسمح بشهادة بديلة بعد الإبطال)، لكن لا دالّة ولا مسار حالياً. أُزيلت من المرحلة ١ عمداً: وضعها في المستودع كان يخلط الطبقات ويُدخل نموذج تصريح ثانياً بجانب `[Flags] SystemPermissions`. |
| 6 | **`ICertificateNumberService.SyncCounterWithExistingAsync` بلا مستدعٍ** — المطلوب: زر في الإعدادات بصلاحية مدير، يُستدعى **مرة واحدة** بعد ترحيل الشهادات الورقية، ويعرض عدد السنوات التي تغيّرت (القيمة المُعادة). | مرحلة الواجهة | بدونه يبدأ العدّاد من `0001` ويتضارب مع الأرشيف الورقي للمهندس رضا — أرقام مكررة على وثائق حقيقية. **لا يُستدعى في كل إقلاع**: المزامنة لا تُنقص العدّاد، لكن استدعاءها الدوري يُخفي أخطاء الترحيل بدل كشفها. |

### 10.1 بنود مؤجلة

| البند | الحالة |
|---|---|
| إعادة تصميم محتوى الـ QR للشهادة الكاملة | مؤجل حتى اكتمال النماذج الخمسة |
| تعريف حقول قسمي "النتائج" و"عدم اليقين" للأنواع الثلاثة المتبقية | بانتظار المهندس رضا |
| هل قسم "منهجية المعايرة" خاص بأجهزة جاما أم عام؟ | بانتظار التأكيد |
| إعادة تسمية بطاقة لوحة التحكم إلى "إجمالي الكواشف المُعايَرة" | جاهز للتنفيذ |
| فلتر السنة في تبويب السجلات | جاهز للتنفيذ (مستقل تماماً) |

---

## القسم 11: خطة التنفيذ المعتمدة — مساران متوازيان

### المسار (أ) — يبدأ فوراً، لا يعتمد على المهندس رضا
1. كيان `Certificate` بالهيكل العظمي + الربط بـ `CalibrationRecord`
2. خدمة الترقيم `TNRC-SSDL-YYYY-XXXX` بالالتزام المتأخر + التصفير السنوي
3. الأقسام الثابتة: الترويسة، بيانات العميل والجهاز، الظروف البيئية، التوقيعات الأربعة، التذييل
4. الجداول الأبناء الثلاثة (بنية فارغة قابلة للتعبئة)
5. إعدادات الموقّعين الافتراضيين في `AppSettings`
6. فلتر السنة في تبويب السجلات (بند مستقل)

### المسار (ب) — ينتظر النماذج الثلاثة المتبقية
1. تعريفات حقول "النتائج" و"عدم اليقين" لكل نوع
2. القوائم الافتراضية للفحوصات الوظيفية لكل نوع
3. حسم نطاق قسم "منهجية المعايرة"
4. تصميم محتوى الـ QR النهائي

---

## القسم 12: قواعد الحوكمة السارية (تذكير)

1. لا `git commit` ولا `git push` بدون تفويض صريح لكل رسالة من م. إدريس.
2. Antigravity لا يشغّل التطبيق ولا يلتقط صور الشاشة.
3. `dotnet build` / `dotnet test` تتطلب موافقة يدوية داخل واجهة Antigravity.
4. Claude يتحقق من الكود الفعلي أو الإخراج الخام — لا من التقارير المُلخّصة.
5. الالتزام التام بالقواعد المعمارية الصارمة الإحدى عشرة في `ProjectContext.md`.
